{
  "name": "painting-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('Open index.html directly or use VS Code Live Server.')\"",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test"
  }
}
